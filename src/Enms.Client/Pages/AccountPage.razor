@page "/account"

@namespace Enms.Client.Pages

@attribute [Navigation(Title = "Account", Order = 100)]
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@using Enms.Business.Models.Composite
@using Enms.Client.Shared.Models
@using Enms.Client.State
@inherits Enms.Client.Base.EnmsOwningComponentBase

<MaybeRepresentingUserDetails Model="@(new MaybeRepresentingUserModel(user.User) { MaybeRepresentative = representative?.Representative })"/>

@code {
  [CascadingParameter]
  public UserState user { get; set; } = default!;

  [CascadingParameter]
  public RepresentativeState? representative { get; set; } = default!;
}
